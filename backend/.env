# Landing 프로젝트 백엔드 환경변수 템플릿
# 실제 사용 시 이 파일을 .env로 복사하고 값을 설정하세요

# 서버 설정
LANDING_PORT=3001
CORS_ORIGIN=http://localhost:3000

# PostgreSQL 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=funnel_analytics
DB_USER=postgres
DB_PASSWORD=solomon373@!

# 보안 설정 (이건 나중에)
NODE_ENV=development
# JWT_SECRET=your_jwt_secret_here

# API 제한 설정
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 로깅 설정
LOG_LEVEL=info
LOG_FILE=logs/landing-backend.log

# Landing 프로젝트 특화 설정
LANDING_ANALYTICS_ENABLED=true
LANDING_SESSION_TIMEOUT=3600000
LANDING_BATCH_SIZE=50

# Survey 연동 설정
SURVEY_BASE_URL=http://localhost:3002
ENABLE_SURVEY_INTEGRATION=true